
def average_age():
    # Get User Input
    age1 = float(input('Enter age 1: '))
    age2 = float(input('Enter age 2: '))
    age3 = float(input('Enter age 3: '))
    age4 = float(input('Enter age 4: '))
    age5 = float(input('Enter age 5: '))
    average_age = (age1 + age2 + age3 + age4 + age5) / 5
    print('The average age is:', average_age)

    # Average the ages

    # Print the results

# Line which calls the above function.
average_age()